"""critic_react_v3 — single ReAct agent + open-question ledger + adversarial critic.

Public exports are wired in Task 9 once the runner module exists. Until then this
file intentionally exposes nothing — keep imports out so partial-state collection
doesn't fail.
"""
